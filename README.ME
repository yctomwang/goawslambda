here is the code for a simple golang webhook that will be running lambda which will pump the user submitted code into the SQS which allows the backend distributed golang workers to pick up and execute.
